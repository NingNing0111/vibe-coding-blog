# ============================================
# 数据库配置
# ============================================
# Docker 环境使用 'postgres' 作为主机名，本地开发使用 'localhost'
# 生产环境请修改为实际的数据库连接信息
DATABASE_URL=postgresql+asyncpg://blog_user:blog_password@localhost:5432/blog_db

# ============================================
# Redis 配置
# ============================================
# Docker 环境使用 'redis' 作为主机名，本地开发使用 'localhost'
# 生产环境请修改为实际的 Redis 连接信息
REDIS_URL=redis://localhost:6379/0

# ============================================
# JWT 配置
# ============================================
# 生产环境必须修改为强随机密钥
SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# CORS 配置
# ============================================
# 多个域名用逗号分隔，例如: http://localhost:3000,https://example.com
CORS_ORIGINS=http://localhost:3000

# ============================================
# S3 配置（可选）
# ============================================
# 如果使用 S3 存储文件，请填写以下配置
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET_NAME=

# ============================================
# 邮件配置（可选）
# ============================================
# 如果使用邮件服务，请填写以下配置
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=

# ============================================
# OpenAI 配置（可选）
# ============================================
# 如果使用 AI 功能，请填写以下配置
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

# ============================================
# 前端配置
# ============================================
# 前端 API 地址，Docker 环境通常为 http://localhost/api
NEXT_PUBLIC_API_URL=http://localhost:8000
