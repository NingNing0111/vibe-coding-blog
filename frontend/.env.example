# ============================================
# 注意：本项目已统一使用项目根目录的 .env 文件
# ============================================
# 请直接编辑项目根目录的 .env 文件，而不是此文件
# 此文件仅作为参考，保留用于向后兼容
# ============================================
#
# 前端 API 配置（与根目录 .env 保持一致）：
# - 同域部署（前后端同一域名，后端挂在 /api）：填 /api，部署时配置此项即可
#   另在根目录 .env 设 NEXT_PUBLIC_SITE_ORIGIN=站点地址（如 https://你的域名）
# - 本地/跨域开发：填完整地址，如 http://localhost:8000
#
# ============================================

NEXT_PUBLIC_API_URL=http://localhost:8000
